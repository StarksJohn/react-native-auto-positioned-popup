# NPM configuration for react-native-auto-positioned-popup

# Use legacy peer dependency algorithm to avoid conflicts
# This is necessary due to React Native's peer dependency requirements
legacy-peer-deps=true

# Disable fund messages
fund=false

# Set audit level to moderate
audit-level=moderate